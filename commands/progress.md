---
description: "Map the full arc of your coaching journey: where you started, what shifted, patterns resolved, patterns remaining, and what's next."
---

Invoke the `life-coach:life-coach` skill using the Skill tool, then execute the `/progress` command flow from `command-handlers/progress-accountability.md`.
