---
description: "Map the full arc of your coaching journey: where you started, what shifted, patterns resolved, patterns remaining, and what's next."
---

Read `${CLAUDE_PLUGIN_ROOT}/SKILL.md` for the full session orchestration context, then read `${CLAUDE_PLUGIN_ROOT}/command-handlers/progress-accountability.md` and execute the `/progress` command flow defined there.
