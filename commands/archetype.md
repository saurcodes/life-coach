---
description: "Reveal and verify the client archetype detected so far (Achiever, Seeker, Caregiver, Analyst, Rebel, etc.) and how it shapes the coaching approach."
---

Invoke the `life-coach:life-coach` skill using the Skill tool, then execute the `/archetype` command flow from `command-handlers/diagnostic.md`.
