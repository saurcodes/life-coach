---
description: "Reveal and verify the client archetype detected so far (Achiever, Seeker, Caregiver, Analyst, Rebel, etc.) and how it shapes the coaching approach."
---

Read `${CLAUDE_PLUGIN_ROOT}/SKILL.md` for the full session orchestration context, then read `${CLAUDE_PLUGIN_ROOT}/command-handlers/diagnostic.md` and execute the `/archetype` command flow defined there.
