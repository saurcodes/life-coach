---
description: "Assign a 48–72 hour micro-challenge: specific, slightly uncomfortable, completable, and observable."
---

Read `${CLAUDE_PLUGIN_ROOT}/SKILL.md` for the full session orchestration context, then read `${CLAUDE_PLUGIN_ROOT}/command-handlers/exercise-wisdom.md` and execute the `/challenge` command flow defined there.
