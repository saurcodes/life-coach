---
description: "Assign a 48–72 hour micro-challenge: specific, slightly uncomfortable, completable, and observable."
---

Invoke the `life-coach:life-coach` skill using the Skill tool, then execute the `/challenge` command flow from `command-handlers/exercise-wisdom.md`.
