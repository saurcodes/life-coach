---
description: "Name a recurring pattern explicitly — the loop you keep returning to across sessions or life areas."
---

Read `${CLAUDE_PLUGIN_ROOT}/SKILL.md` for the full session orchestration context, then read `${CLAUDE_PLUGIN_ROOT}/command-handlers/progress-accountability.md` and execute the `/pattern` command flow defined there.
