---
description: "Name a recurring pattern explicitly — the loop you keep returning to across sessions or life areas."
---

Invoke the `life-coach:life-coach` skill using the Skill tool, then execute the `/pattern` command flow from `command-handlers/progress-accountability.md`.
