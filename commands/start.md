---
description: "Begin a new life coaching session. Runs continuity check first — if prior session exists, opens with accountability review. Otherwise begins archetype detection and discovery."
---

Read `${CLAUDE_PLUGIN_ROOT}/SKILL.md` for the full session orchestration context, then read `${CLAUDE_PLUGIN_ROOT}/command-handlers/session-management.md` and execute the `/start` command flow defined there.
